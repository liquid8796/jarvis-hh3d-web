# deps / build
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env — NEVER commit secrets
.env
.env.*
!.env.example

# misc
.DS_Store
.vercel

# Cấu hình tooling của người viết mã — riêng của từng máy, không thuộc về repo dùng chung.
.claude/

# Gói khôi lỗi — sinh ra ở prebuild từ scripts/buildWorkerBundle.mjs, không commit
/public/linh-su/goi-linh-su.tgz

